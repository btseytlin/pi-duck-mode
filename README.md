# pi-duck-mode

A pi extension for a coding harness with no LLM.
Every message gets the same reply: "Great, go ahead and make it!"
Run `./duck` for a session that is always duck mode.
Or type `/duck` in a normal pi session to toggle duck mode on and off.
All commands, hotkeys and tools are off while it is on.
